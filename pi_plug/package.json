{
  "name": "pi_plug",
  "version": "1.0.0",
  "description": "- Install git `sudo apt install git` - Install Node `sudo apt install nodejs` - Install Wireguard ` sudo apt install wireguard`",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "rpio": "^2.4.2"
  }
}
